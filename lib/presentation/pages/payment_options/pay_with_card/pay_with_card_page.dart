import 'package:flutter/material.dart';

class PayWithCardPage extends StatelessWidget {
  const PayWithCardPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}
