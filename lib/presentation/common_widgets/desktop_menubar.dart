import 'package:flutter/material.dart';

class DesktopMenuBar extends StatelessWidget {
  const DesktopMenuBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 50,
      color: Colors.pink,
    );
  }
}
